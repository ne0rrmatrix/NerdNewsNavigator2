<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NerdNewsNavigator2.View.SettingsPage"
            xmlns:model="clr-namespace:NerdNewsNavigator2.Model"
             xmlns:viewmodel="clr-namespace:NerdNewsNavigator2.ViewModel"
             x:DataType="viewmodel:SettingsViewModel"
             BackgroundColor="Wheat"
               Shell.NavBarIsVisible="False"
             Title="">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="True" IsVisible="False"></BackButtonBehavior>
    </Shell.BackButtonBehavior>
    
    <CollectionView ItemsSource="{Binding Files}"  BackgroundColor="Wheat" SelectionMode="None">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical"  Span="4"/>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:JsonData">


                <Frame BackgroundColor="Wheat" Margin="55">
                    <Grid RowDefinitions="45,0" ColumnDefinitions="220,70">
                    
                        <Label Grid.Row="0" Margin="2" VerticalOptions="Center" HorizontalOptions="Center"
                        Text="{Binding Url}"
                        TextColor="Black" 
                        FontSize="10" 
                            FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>


                        <Button Text="Delete" TextColor="White" BackgroundColor="DarkRed" FontAttributes="Bold" WidthRequest="80" HeightRequest="10" Grid.Column="1">
                            <Button.GestureRecognizers>
                                <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:SettingsViewModel}}, Path=TapCommand}"
                            CommandParameter="{Binding Id}"/>
                            </Button.GestureRecognizers>
                        </Button>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>