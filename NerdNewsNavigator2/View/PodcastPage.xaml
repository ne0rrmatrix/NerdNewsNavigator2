<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NerdNewsNavigator2.View.PodcastPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:NerdNewsNavigator2.Model"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodel="clr-namespace:NerdNewsNavigator2.ViewModel"
    xmlns:woka="clr-namespace:Woka.Controls;assembly=Woka"
    Title="{Binding Title}"
    x:DataType="viewmodel:PodcastViewModel"
    BackgroundColor="#34AAD2"
    Loaded="ContentPage_Loaded">

    <RefreshView Command="{Binding PullToRefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
        <CollectionView
            Margin="5"
            HorizontalScrollBarVisibility="Never"
            ItemsSource="{Binding Podcasts}"
            VerticalScrollBarVisibility="Never">

            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    x:Name="ItemLayout"
                    Orientation="Vertical"
                    Span="{Binding Orientation}" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Podcast">
                    <Frame
                        Margin="{OnPlatform WinUI=80,
                                            MacCatalyst=10,
                                            Android=5,
                                            iOS=90}"
                        Padding="{OnPlatform Default=20,
                                             iOS=10}"
                        BackgroundColor="White"
                        WidthRequest="{OnPlatform WinUI=370,
                                                  MacCatalyst=340,
                                                  Android=320,
                                                  iOS=340}">

                        <woka:WokaGrid HorizontalOptions="Center">
                            <woka:WokaGrid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                            </woka:WokaGrid.ColumnDefinitions>

                            <woka:WokaGrid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </woka:WokaGrid.RowDefinitions>

                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:PodcastViewModel}}, Path=GotoShowPageCommand}" CommandParameter="{Binding Url}" />
                            </Grid.GestureRecognizers>

                            <Label
                                Grid.Row="0"
                                Margin="5"
                                Padding="10"
                                FontAttributes="Bold"
                                FontSize="Small"
                                HorizontalOptions="Center"
                                HorizontalTextAlignment="Center"
                                LineBreakMode="WordWrap"
                                MinimumHeightRequest="60"
                                Text="{Binding Title}"
                                TextColor="Black" />
                            <Image
                                Grid.Row="1"
                                Margin="10"
                                Aspect="AspectFit"
                                HeightRequest="{OnIdiom Phone=280,
                                                        Tablet=290,
                                                        Desktop=350}"
                                HorizontalOptions="Center"
                                WidthRequest="{OnIdiom Phone=280,
                                                       Tablet=290,
                                                       Desktop=350}">
                                <Image.Source>
                                    <UriImageSource
                                        CacheValidity="10:00:00:00"
                                        CachingEnabled="true"
                                        Uri="{Binding Image}" />
                                </Image.Source>
                            </Image>
                            <Label
                                Grid.Row="2"
                                FontSize="Micro"
                                LineBreakMode="WordWrap"
                                Text="{Binding Description}"
                                TextColor="Black"
                                WidthRequest="{OnIdiom Phone=290,
                                                       Tablet=290,
                                                       Default=290,
                                                       Desktop=340}" />

                        </woka:WokaGrid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <ContentView>
                    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                        <Label
                            Padding="20"
                            FontSize="Large"
                            HorizontalOptions="Center"
                            HorizontalTextAlignment="Center"
                            Text="Loading..."
                            TextColor="Black"
                            VerticalOptions="Center" />
                        <ActivityIndicator IsRunning="True" Color="Red" />
                    </StackLayout>
                </ContentView>
            </CollectionView.EmptyView>
        </CollectionView>

    </RefreshView>
</ContentPage>